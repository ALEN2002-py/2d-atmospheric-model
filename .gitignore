# Generated output — not tracked
output/
*.nc
*.npy
*.npz
*.json
# ...but *.json is too broad for dashboard/, which has real config files
# that happen to be JSON (not generated output) -- un-ignore those specifically.
!dashboard/package.json
!dashboard/package-lock.json
!dashboard/tsconfig*.json

# ML surrogate — regenerable via ml/dataset.py, ml/train.py, ml/evaluate.py
# (dataset is ~135MB, not something to commit; the one figure worth keeping
# is copied to assets/ml_surrogate_comparison.png, committed there instead)
ml/data/
ml/surrogate.pt
ml/surrogate_comparison.png
.venv/

# Private project notes
CLAUDE.md

# Thesis drafts — kept local, not pushed
docs/thesis/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.env

# Jupyter
.ipynb_checkpoints/

# Build / packaging
*.egg-info/
dist/
build/

# OS / editor
.DS_Store
Thumbs.db
.vscode/settings.json
.idea/
